name: Deploy non-prod

on:
  push:
    branches: [develop]
  workflow_dispatch:

concurrency:
  group: deploy-np
  cancel-in-progress: false

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm test

  deploy:
    name: Deploy to didi-dir-np
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: non-prod
      url: https://didi-dir-np.azurewebsites.net
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm

      - name: Install production dependencies
        run: npm ci --omit=dev

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: didi-dir-np
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE_NP }}

      - name: Wait for the deployment to become healthy
        run: |
          for i in $(seq 1 30); do
            body=$(curl -s https://didi-dir-np.azurewebsites.net/healthz || true)
            echo "attempt $i: $body"
            case "$body" in *'"status":"ok"'*) echo "non-prod healthy"; exit 0;; esac
            sleep 10
          done
          echo "non-prod did not report healthy after deploy"; exit 1
